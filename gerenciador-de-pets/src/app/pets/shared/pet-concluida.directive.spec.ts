import { PetConcluidaDirective } from './pet-concluida.directive';

describe('PetConcluidaDirective', () => {
  it('should create an instance', () => {
    const directive = new PetConcluidaDirective();
    expect(directive).toBeTruthy();
  });
});
